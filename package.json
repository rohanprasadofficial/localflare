{
  "name": "localflare-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "playground": "pnpm --filter playground dev",
    "release": "pnpm build && pnpm publish:packages",
    "release:beta": "pnpm build && pnpm publish:beta",
    "publish:packages": "pnpm --filter localflare-core publish --no-git-checks && pnpm --filter localflare-api publish --no-git-checks && pnpm --filter localflare publish --no-git-checks",
    "publish:beta": "pnpm --filter localflare-core publish --no-git-checks --tag beta && pnpm --filter localflare-api publish --no-git-checks --tag beta && pnpm --filter localflare publish --no-git-checks --tag beta"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
